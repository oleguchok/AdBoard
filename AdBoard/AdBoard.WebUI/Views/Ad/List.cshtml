@using AdBoard.Domain.Entities
@model AdBoard.WebUI.Models.AdListViewModel

@{
    ViewBag.Title = "Ads";
}

@foreach (var p in @Model)
{
    <div>
        <h3>@p.Title</h3>
        <p>@p.Description</p>
        <h4>@p.Price.ToString("# rub")</h4>
    </div>
}
